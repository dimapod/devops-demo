---

- name: Copy AWS Credentials
  copy:
    src: ~/s3-credentials.yml
    dest: .

- name: Set Concourse Pipeline
  become: yes
#  become_user: docker
  shell: ./devops-demo/concourse/provision/fly-docker.sh set-pipeline devops $(pwd)/devops-demo/concourse/pipeline/build/pipeline.yml
